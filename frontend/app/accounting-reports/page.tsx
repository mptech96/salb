"use client";
import {useEffect,useState} from "react";import api from "../api";import SystemDialog from "@/components/common/SystemDialog";const m=(x:any)=>Number(x||0).toFixed(3);
export default function AccountingReportsPage(){const[tab,setTab]=useState('trial-balance'),[data,setData]=useState<any>(null),[dlg,setDlg]=useState<any>({open:false,title:'',message:''});const load=async()=>{try{const r=await api.get(`/accounting/${tab}`);setData(r.data.data)}catch(e:any){setDlg({open:true,title:'تعذر تحميل التقرير',message:e?.response?.data?.message||'حدث خطأ'})}};useEffect(()=>{void load()},[tab]);return <section dir="rtl" className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3 rounded-3xl bg-[#0B2A4A] p-6 text-white"><div><div className="text-sm text-blue-100">القوائم المالية</div><h1 className="text-3xl font-black">التقارير المحاسبية</h1></div><button onClick={()=>window.print()} className="rounded-xl bg-white px-4 py-2 font-black text-[#0B2A4A]">طباعة</button></div><div className="flex flex-wrap gap-2 rounded-2xl border bg-white p-2">{[['trial-balance','ميزان المراجعة'],['income-statement','قائمة الدخل'],['balance-sheet','المركز المالي']].map(([k,l])=><button key={k} onClick={()=>setTab(k)} className={`rounded-xl px-4 py-2 font-bold ${tab===k?'bg-[#0B2A4A] text-white':'bg-slate-100'}`}>{l}</button>)}</div>{tab==='trial-balance'&&data&&<div className="overflow-x-auto rounded-3xl border bg-white"><table className="w-full min-w-[950px] text-right"><thead className="bg-slate-100"><tr><th className="p-3">الحساب</th><th>افتتاحي مدين</th><th>افتتاحي دائن</th><th>حركة مدين</th><th>حركة دائن</th><th>ختامي مدين</th><th>ختامي دائن</th></tr></thead><tbody>{data.rows?.map((r:any)=><tr key={r.account_id} className="border-t"><td className="p-3 font-bold">{r.account_code} - {r.account_name}</td><td>{m(r.opening_debit)}</td><td>{m(r.opening_credit)}</td><td>{m(r.period_debit)}</td><td>{m(r.period_credit)}</td><td>{m(r.closing_debit)}</td><td>{m(r.closing_credit)}</td></tr>)}</tbody><tfoot className="bg-slate-50 font-black"><tr><td className="p-3">الإجمالي</td><td>{m(data.totals?.opening_debit)}</td><td>{m(data.totals?.opening_credit)}</td><td>{m(data.totals?.period_debit)}</td><td>{m(data.totals?.period_credit)}</td><td>{m(data.totals?.closing_debit)}</td><td>{m(data.totals?.closing_credit)}</td></tr></tfoot></table></div>}{tab==='income-statement'&&data&&<div className="grid gap-4 xl:grid-cols-3">{[['الإيرادات',data.revenues,data.revenue_total],['تكلفة الإيرادات',data.cost_of_revenue,data.cost_of_revenue_total],['المصروفات التشغيلية',data.operating_expenses,data.operating_expenses_total]].map(([t,rows,total]:any)=><div key={t} className="rounded-3xl border bg-white p-5"><h2 className="text-xl font-black text-[#0B2A4A]">{t}</h2>{rows?.map((r:any)=><div key={r.account_id} className="mt-3 flex justify-between border-b pb-2"><span>{r.account_code} - {r.account_name}</span><b>{m(r.amount)}</b></div>)}<div className="mt-4 flex justify-between text-lg font-black"><span>الإجمالي</span><span>{m(total)}</span></div></div>)}<div className="xl:col-span-3 rounded-3xl bg-[#0B2A4A] p-5 text-white"><div>مجمل الربح: {m(data.gross_profit)}</div><div className="mt-2 text-2xl font-black">صافي الربح / الخسارة: {m(data.net_result)}</div></div></div>}{tab==='balance-sheet'&&data&&<div className="grid gap-4 xl:grid-cols-3">{[['الأصول',data.assets,data.total_assets],['الالتزامات',data.liabilities,data.total_liabilities],['حقوق الملكية',data.equity,data.total_equity]].map(([t,rows,total]:any)=><div key={t} className="rounded-3xl border bg-white p-5"><h2 className="text-xl font-black text-[#0B2A4A]">{t}</h2>{rows?.map((r:any)=><div key={r.account_id} className="mt-3 flex justify-between border-b pb-2"><span>{r.account_code} - {r.account_name}</span><b>{m(r.amount)}</b></div>)}{t==='حقوق الملكية'&&Number(data.current_period_result)!==0&&<div className="mt-3 flex justify-between border-b pb-2"><span>نتيجة الفترة الحالية</span><b>{m(data.current_period_result)}</b></div>}<div className="mt-4 flex justify-between text-lg font-black"><span>الإجمالي</span><span>{m(total)}</span></div></div>)}<div className={`xl:col-span-3 rounded-3xl p-5 font-black ${Math.abs(Number(data.difference||0))<.001?'bg-emerald-50 text-emerald-800':'bg-rose-50 text-rose-800'}`}>الأصول {m(data.total_assets)} | الالتزامات وحقوق الملكية {m(data.total_liabilities_equity)} | الفرق {m(data.difference)}</div></div>}<SystemDialog open={dlg.open} type="error" title={dlg.title} message={dlg.message} onClose={()=>setDlg({...dlg,open:false})} onConfirm={()=>setDlg({...dlg,open:false})}/></section>}
